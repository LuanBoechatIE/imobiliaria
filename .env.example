# Chave usada para assinar o cookie de sessão. Mínimo 32 caracteres.
# Gere uma nova com:  node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
# Precisa estar configurada também na Vercel (Project Settings > Environment Variables).
SESSION_SECRET=
